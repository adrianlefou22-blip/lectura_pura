<!-- 
  Este archivo documenta los permisos que se agregan manualmente
  al AndroidManifest.xml después de que Capacitor genere el proyecto Android.
  
  El GitHub Action los aplica automáticamente con el script patch-manifest.py
-->

PERMISOS A AGREGAR:
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

SERVICIO A AGREGAR dentro de <application>:
  <service
      android:name=".AudioForegroundService"
      android:foregroundServiceType="mediaPlayback"
      android:exported="false" />
